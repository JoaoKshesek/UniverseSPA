const page = document.querySelector('body')
const homeButton = document.querySelector('.home-link');
const universeButton = document.querySelector('.universe-link');
const explorationButton = document.querySelector('.exploration-link');

export {
    page,
    homeButton,
    universeButton,
    explorationButton,
}